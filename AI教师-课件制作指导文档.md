# AI 教师项目需求与课件制作指导文档

> **本文档是 AI 教师项目的唯一权威文档**，含三大部分：①课件制作规范与教学设计原则（第 4 章）；②数据架构与单一数据源规范（第 5 章）；③回顾系统规范（第 6 章）。所有课件必须遵循本文档定义的结构、交互、索引、数据和验收标准。文档中"用户明确需求"为硬约束，"方案建议"待确认后生效（已确认项见第 10 章决策记录）。
>
> 本文档为**活文档**，随项目推进持续更新。每次新增课件类型、交互模式或组织方式变更时，应先修订本文档，再执行制作。

---

## 1. 项目概述

**AI 教师**是一套本地化、可交互的小学四科（语文、数学、英语、自然科学）知识学习系统。核心载体是**知识点 HTML 动画课件**——每个知识点制作一份独立的 HTML 课件，通过动画演示、语音讲解、动手操作和即时反馈帮助孩子理解抽象概念。

### 1.1 系统定位

- **单一用户**：本系统只为**自家一个孩子**服务（当前刚升四年级），不做多账号/多孩子隔离
- **内容策略 = 教材全量 + 按需补充**：
  1. **教材全量汇总**：按教材版本，把小学 1–6 年级四科的知识点全量梳理进单一数据源（初始多为"待制作"），形成完整知识地图，作为"整个小学阶段知识汇总"的底座
  2. **按需动态丰富**：孩子学习过程中遇到的具体问题，随时以"知识点"形式补充进清单并制作课件，用 `source:"custom"` 标记，与教材体系区分
- **制作优先级**：孩子当前升四年级，但**各科基础均不扎实**，课件制作两条线并进：① 4 年级当前内容（四上按新版教材、含 3 下衔接）；② **低年级基础补缺**（借助前置知识点追溯薄弱环节，优先补 1–3 年级关键基础）。1–6 年级全量清单先行建立

### 1.2 系统分层

系统分为三层：

- **内容层（课件工厂）**：按统一规范批量生产知识点课件
- **管理层（组织检索）**：所有课件有序存放，通过索引条目快速定位
- **闭环层（回顾系统）**：记录学习过程，支持错题复习和薄弱点巩固

### 1.3 目标学习者画像

> 本系统为一位**四年级男孩**定制，以下画像决定了第 4.9 节全部设计原则，是课件的硬约束。

- 男孩，四年级，各科基础不扎实
- **贪玩**：游戏、动画、动手操作的吸引力 >> 听讲、阅读
- **注意力易分散**：被动观看/长时间讲解容易走神，但**主动操作时专注力明显更好**
- 含义：课件的敌人不是"难"，是"无聊"和"等待"；课件的好友是"任务感""即时反馈""动手感"

### 1.4 教材版本核实（宁波，2026-08-30）

| 学科 | 版本 | 说明 |
|------|------|------|
| 语文 | 人教部编（统编版） | 全国统一，宁波各年级均为人教版/部编版 |
| 数学 | 人教版 | 宁波 1–6 年级数学用人教版（浙江部分地市用北师大，宁波不用） |
| 英语 | 人教 PEP（一年级起点，浙江版） | 宁波小学 **1 年级起开英语课**：1–2 年级用「人教 PEP 一年级起点」（2024/2025 新版，浙江版），3–6 年级延续 PEP 体系。故英语知识清单**从一年级开始建**，覆盖 1–6 年级 |
| 自然科学（科学） | 教科版（教育科学出版社） | 浙江省小学科学统一用**教科版**（2026 年已核浙江省用书目录与教材培训会）。课程按 2022 课标四大领域组织：物质科学、生命科学、地球与宇宙科学、技术与工程。**2026 年秋季四年级上册启用新版教材**（见下方改版说明） |

**2026 年秋季（孩子升四年级）教材改版关键点：**

- **语文四上、数学四上启用新版教材**（按 2022 版课标修订）：语文替换/新增多篇课文（如新增《长城》《田忌赛马》《方帽子店》等）；数学由旧版 9 个单元调整为 6 个主体单元 + 2 个综合实践 + 复习与关联（如"公顷和平方千米"移至五上、新增"加法模型和乘法模型"数量关系单元）
- **英语四年级不在本次改版范围**（本次换新的是五、六年级英语）
- **科学四上启用 2026 秋新版教材**（2022 课标修订版）：由旧版「声音 / 呼吸与消化 / 运动和力」三个单元重构为「空气 / 呼吸与消化 / 声音」三个单元（每单元 8 课）。旧版第三单元"运动和力"移出本册，"声音"由第一单元移至第三单元并重构课时，新增强化"技术与工程"的设计制作课（自制打气筒、设计乐器、模拟安装照明电路等）。**四年级下册仍按「植物的生长变化 / 电路 / 岩石与土壤」三单元（每单元 8 课），2027 春启用时以实际下发课本为准做最终校对**
- ⚠️ 上述改版细节来自公开报道与自媒体汇总，个别条目存在口径差异；**梳理知识点清单前，以孩子实际下发的课本为准做最终校对**

---

## 2. 核心需求清单

以下为用户明确提出的需求，是项目不可降级的硬约束：

| 编号 | 需求 | 说明 |
|------|------|------|
| R1 | 覆盖小学四科 | 语文、数学、英语、自然科学，面向小学 1–6 年级 |
| R2 | 每知识点一个 HTML 动画课件 | 针对每个知识点制作专门的 HTML 动画课件，不混用、不泛化 |
| R3 | 有效组织材料，支持后续回顾 | 所有课件有序组织，可检索、可回顾学习记录 |
| R4 | 语音讲解 | 课件动画配有语音讲解，孩子可听可看 |
| R5 | 步骤控制 + 自动播放 | 可手动按步骤控制，也可一键自动播放 |
| R6 | 索引条目 + 点击跳转 | 每个学科每个知识点有索引条目，点击查看具体课件 |

---

## 3. 知识体系

### 3.1 四科模块划分

按"学科 → 模块 → 知识点"三层组织，覆盖 1–6 年级：

| 学科 | 知识模块 | 代表性知识点（示例） |
|------|----------|----------------------|
| **语文** | 拼音与识字 | 声母韵母、整体认读音节、笔画笔顺、偏旁部首、多音字 |
| | 词语与句子 | 近反义词、量词、关联词、成语、修辞手法、修改病句 |
| | 阅读 | 记叙文阅读、古诗文阅读、说明文阅读 |
| | 写作表达 | 看图写话、写人记事、日记与书信 |
| | 古诗文积累 | 必背古诗、文言文启蒙 |
| **数学** | 数与运算 | 整数四则运算、小数、分数、百分数、简便运算 |
| | 量与计量 | 长度重量时间单位换算、钟表与时间 |
| | 图形与几何 | 平面图形、立体图形、周长面积体积 |
| | 统计与概率 | 统计图表、平均数、可能性 |
| | 典型应用 | 应用题、行程问题、植树问题、鸡兔同笼、找规律 |
| **英语** | 语音 | 自然拼读、音标入门、重音与语调 |
| | 词汇 | 话题词汇、词性认知、常用短语 |
| | 语法 | be 动词、时态、句型结构 |
| | 听说 | 情景对话、课堂用语 |
| | 读写 | 阅读理解、看图写话 |
| **自然科学** | 物质科学 | 空气与水、声音、热、电、磁、光的现象、物质变化 |
| | 生命科学 | 人体（呼吸、消化、健康）、植物、动物、生物与环境 |
| | 地球与宇宙科学 | 岩石矿物、土壤、天气、地球与宇宙、地表变化 |
| | 技术与工程 | 设计与制作（自制打气筒、小乐器、照明电路、小车等） |

### 3.2 知识点粒度规范

- 一个知识点 = **一次能讲清、约 10 分钟内能学完**的最小教学单元
- 例如"分数"是大主题，应拆分为"分数的初步认识""同分母分数加减""分数与小数互化"等独立知识点
- 每个知识点对应**一份独立课件**和**一条索引条目**

### 3.3 知识点元数据（语义层）

每个知识点必须维护以下元数据，用于索引和检索（**具体数据字段与格式见 5.3.3**）：

| 字段 | 必填 | 说明 |
|------|------|------|
| 知识点 ID | 是 | 唯一标识，格式：`学科-年级-序号`，如 `math-3-012` |
| 知识点名称 | 是 | 简洁明确，如"同分母分数加减法" |
| 学科 | 是 | 语文 / 数学 / 英语 |
| 年级 | 是 | 1–6 年级 |
| 学期/单元 | 是 | 对应教材的学期和单元 |
| 难度 | 是 | 基础 / 提高 / 挑战 |
| 前置知识点 | 否 | 学习本知识点前需掌握的内容，用于知识图谱 |
| 状态 | 是 | 待制作 / 已完成 / 待修订 |
| 课件路径 | 完成后必填 | 对应 HTML 文件的相对路径 |
| 一句话简介 | 是 | 索引条目展示用，不超过 30 字 |

---

## 4. 课件制作规范

> **本节是每个课件制作的核心模板。** 所有课件必须遵循统一的结构、交互和控件规范，确保风格一致、质量可控。
>
> **教学设计补充（硬约束）**：所有课件同时遵循 **4.9 专注友好性设计原则**（面向贪玩、注意力易分散的男孩学习者，采用**轻量游戏化档位**）。

### 4.1 课件整体结构：步骤播放器

每份课件是一个**步骤播放器（Step Player）**，内容按有序步骤组织，用户可手动步进或自动播放。

标准内容分为五段，每段包含一个或多个步骤：

1. **情境引入**（1–2 步）：用生活场景或问题引出知识点
2. **动画讲解**（2–4 步）：核心概念，通过动画逐步演示
3. **例题演示**（1–3 步）：典型例题，分步动画解答
4. **互动练习**（1–3 步）：穿插小题，即时判分反馈
5. **要点小结**（1 步）：总结核心结论和易错点

> **节奏补充（面向目标学习者）**：每段需进一步拆为**微环节**，每个微环节 ≤90 秒完整闭环（任务预告 → 看/听 → 操作 → 反馈 → 小奖励）；每步开始给 1 句任务预告（如"接下来帮小猴把苹果平均分！"）。

### 4.2 步骤定义

每个步骤是播放器的最小单元，包含：

- **动画画面**：SVG/CSS/Canvas 动画，展示当前步骤的视觉内容
- **讲解文本**：当前步骤的语音讲解文字，同时显示在画面旁（充当字幕）
- **互动操作**（可选）：选择题、填空题、拖动操作等，需即时反馈

### 4.3 控制栏规范

每份课件底部必须包含统一控制栏，控件如下：

| 控件 | 必填 | 行为规范 |
|------|------|----------|
| 上一步 | 是 | 回到上一步，第一步时禁用；触发语音讲解当前步骤 |
| 下一步 | 是 | 进入下一步，最后一步时禁用；触发语音讲解当前步骤 |
| 播放 / 暂停 | 是 | 点击后自动依次播放所有步骤；播放中按钮变为"暂停"，颜色变红提示 |
| 重置 | 是 | 回到第一步，停止播放和语音 |
| 步骤进度条 | 是 | 显示总步数和当前位置，**可点击任意位置跳转** |
| 语速调节 | 是 | 慢 / 正常 / 快 三档，影响语音语速和自动播放停留时长 |
| 语音开关 | 是 | 开启/关闭语音讲解，关闭时不朗读但讲解文本仍显示 |

### 4.4 语音讲解规范

- **首选技术**：Web Speech API（`SpeechSynthesis`），浏览器内置中文 TTS，零音频文件、修改文本即修改语音
- **语言**：中文（`zh-CN`），英语课件的英文单词/句子用英文朗读（`en-US`）
- **语速**：默认 1.0（正常），支持 0.7（慢）、1.3（快）
- **字幕**：讲解文本**始终显示**在画面旁，听不清也能阅读
- **替换接口**：对语文拼音、英语发音等音色要求高的场景，预留预录 MP3 替换能力（同一套播放控制，仅换音源）
- **中断机制**：切换步骤或暂停时，立即中断当前语音（`speechSynthesis.cancel()`）
- **已知风险（实现时必须处理）**：
  - Chrome 朗读长文本有"读到一半自动停止"的已知问题 → 讲解文本建议 ≤120 字，或自动分段朗读
  - 首次 `speak` 常有延迟/无声 → 页面加载时先预初始化语音引擎
  - 不同浏览器中文音色差异大 → 验收时在 Chrome + Safari 双浏览器检查语音；完全无 TTS 时降级为纯字幕可读

### 4.5 自动播放规范

- 点击"播放"后：朗读当前步骤 → 停留（时长随语速缩放，默认约 2.5 秒）→ 自动进入下一步 → 直到最后一步自动停止
- 播放中可随时暂停、跳转、重置，操作后状态即时更新
- 自动播放到最后一步时，按钮恢复为"播放"，提示播放结束
- 若当前步骤包含互动操作题，自动播放应**暂停等待用户作答**，作答后继续（或提供"跳过"按钮）

### 4.6 动画呈现规范

- **【核心原则】知识原理优先用动画演示**：每个知识点的核心概念、规则、原理，必须用至少一个动画演示其"形成过程"（分级怎么分、数位怎么进位、0 怎么占位、图形怎么变化、运算规律怎么发生），能动的原理不要静着讲；同一知识点可叠加多个子动画拆解原理
- 优先使用纯 CSS / SVG / Canvas 动画，零外部依赖
- 动画必须服务于概念理解，不做无意义装饰
- 操作类动画（拖动、点击、滑块）必须提供即时视觉反馈
- 几何关系（垂直、平行、等分、旋转）必须准确，不能"差一点"
- 动画可暂停、可重置，不持续高频空转

### 4.7 互动练习规范

- 题目嵌入步骤流中，讲到哪练到哪，末尾可设综合练习
- 每知识点至少 **2 道**互动练习，题型偏操作：拖拽匹配、翻卡、射击式选择、点击选择、填空题
- 作答后**即时反馈**：正确显示绿色鼓励 + 短庆祝（≤2 秒）+ 多变鼓励语音；错误**温和提示**（不用刺眼红色），给"再试一次"选项，第二次仍错才给出正确答案和讲解；错误次数等挫败性信息只展示在家长端
- 练习结果记入本地学习记录（见第 6 章回顾系统）

### 4.8 难度分层

每份课件内的练习和例题按三档组织：

- **基础**：直接套用概念，确保理解
- **提高**：需要一定推理或组合运用
- **挑战**：拓展思维，面向学有余力的孩子；包装为"Boss 关"叙事，答对给特殊奖励（金币/徽章），失败可重来并给提示

### 4.9 专注友好性设计原则（针对贪玩、注意力易分散的男孩）

> 核心哲学：**把学习做成"闯关任务"，游戏为学习服务**——短周期、高频反馈、强操作、弱噪音，让孩子在走神之前完成一个个小闭环。

#### 4.9.1 时间与节奏

| 项 | 建议 | 说明 |
|----|------|------|
| 单知识点时长 | ≤10 分钟 | 上限不变，但内部结构要碎 |
| **微环节** | 每知识点拆 4–6 个微环节，每个 **≤90 秒完整闭环** | 闭环 = 任务预告 → 看/听 → 操作 → 反馈 → 小奖励，90 秒内走完 |
| 自动播放停留 | 默认 **2.5s**（随语速缩放） | 减少空等，避免等待中走神 |
| 步骤任务预告 | 每步开始先用 1 句话预告"我们要做什么" | 如"接下来帮小猴把苹果平均分！"，给孩子一个明确期待 |
| 环节完成反馈 | 每完成一个微环节给 1–2s 小庆祝 | 及时兑现成就感，不攒到最后 |

#### 4.9.2 交互优先（最重要的原则）

- **"能动手就不干看"**：讲解类步骤尽量改造成可操作演示，而不是被动动画
  - 例如"分数的初步认识"不只看切饼动画，而是让孩子**亲手拖动切分**并看到每份是多少
- 每个知识点的动画讲解中，**至少 1 次**让孩子参与（点击/拖动/排序/填空）
- 操作反馈**立即且明显**（变形、变色、短音效），不做"没反应的点击"
- 练习数量下限：每知识点 **≥2 道互动练习**，题型偏操作（拖拽匹配、射击式选择、拼图式、翻卡）
- 容错设计：点错不惩罚、不显示错误计数，给"再试一次"或"看提示"选项

#### 4.9.3 游戏化（轻量档·克制版）

- **全局进度**：能量条/星级，学完一个知识点得星/徽章
- **连对激励**：连续答对显示连击（如"3 连击！"）并短庆祝，刺激持续专注
- **挑战档 = Boss 关**：`difficulty:"挑战"` 的题目包成关卡叙事，答对给特殊奖励（金币/徽章）
- **复习/错题叙事化**：薄弱点复习 = "收复城池"，错题 = "待修复的宝藏图"，降低复习的枯燥感
- **红线（防走偏）**：
  - 不引入复杂游戏规则（孩子记不住）
  - 不做随机/赌博式奖励（答对即稳定奖励，靠努力不靠运气）
  - 不制造挫败感（挑战关失败允许重来、给提示，不"游戏结束"式惩罚）
  - 游戏外壳下学习任务感必须清晰（每步都知道自己在学什么）

#### 4.9.4 注意力保护

| 项 | 要求 |
|----|------|
| 单一视觉焦点 | 每步只突出 1 个核心元素，其余弱化/置灰 |
| 界面极简 | 去除与学习无关的装饰元素（继承"动画不做无意义装饰"并强化） |
| 背景音乐 | **默认关闭**（或极轻），音效只用于短反馈；贪玩孩子容易被音乐带跑 |
| 动画不空转 | 所有动画有教学目的，禁止持续高频空转 |
| 语音风格 | 讲解有起伏、亲切、带提问感（如"你猜会怎样？"），吸引注意而非催眠 |
| 打断机制 | 步骤切换/暂停立即中断语音，保证响应零延迟 |

#### 4.9.5 反馈机制

- **答对**：绿色 + 短庆祝（1–1.5s）+ 鼓励语音（**多变、不重复**，避免麻木）
- **答错**：温和提示（不用刺眼红色刺激），给"再试一次"；第二次仍错才给答案与讲解
- **反馈时长**：庆祝 ≤2s，不打断学习节奏
- **挫败信息不外露给孩子**：错误次数、得分对比等统计只在家长端展示

#### 4.9.6 家长陪学支持（家长端 / 陪学提示）

- 每次学习结束给家长**一句话总结**：完成度、正确率、易分心环节、建议下一步（如"第 3 步停留过久，可能是分数概念没衔接上，建议先看前置课件 X"）
- 走神提醒：课件检测到某步停留过久/反复跳转，提示家长"孩子可能走神，可引导暂停、调整坐姿或先休息"
- **每日时长上限**：家长可设置（如 15–20 分钟），到点温和结束并给出"今天已完成"的积极反馈

---

## 5. 材料组织与单一数据源

> 本节将"目录结构 + 文件命名 + 索引系统"与"单一数据源"统一。**核心目标**：建立 `knowledge-base.js` 作为唯一事实来源，让索引、导航、回顾、知识图谱统一从它取数，杜绝"索引有课件没有 / 课件有索引没加"的脱节。

### 5.1 为什么需要单一数据源

原设计中，知识点信息被三处重复维护：元数据（§3.3）、索引条目（索引页）、回顾系统（按 ID 记录）。各自手写维护容易脱节。

引入 `knowledge-base.js` 作为**唯一事实来源**（Single Source of Truth）：所有页面加载它并渲染，新增知识点 = 加一条记录 + 写一个 HTML，索引、搜索、复习推荐自动生效。

### 5.2 目录结构

```
课件库/
├── index.html                    # 导航主页（四科入口 + 最近学习 + 薄弱点 + 全局搜索）
├── 知识图谱.html                 # 【阶段三预留，可选】前置知识点驱动的图谱页
├── assets/
│   ├── knowledge-base.js         # ★ 单一数据源：全部知识点元数据（见 5.4）
│   ├── kb-utils.js               # knowledge-base 的查询函数（见 5.5）
│   ├── player.js                 # 步骤播放器通用组件
│   ├── record.js                 # 回顾系统：localStorage 读写 + 薄弱点判定 + 复习推荐（见第 6 章）
│   ├── style.css                 # 统一样式
│   └── components/               # 可复用动画组件
├── 语文/
│   ├── index.html                # 语文学科索引页（每学科目录内放一个 index.html）
│   ├── 三年级/
│   │   ├── 上册/
│   │   │   ├── 第4单元/
│   │   │   │   └── 数学-三上-第4单元-math-3-012-同分母分数加减法.html
│   │   │   └── ...
│   │   └── ...
│   └── ...
├── 数学/
│   ├── index.html
│   └── ...
└── 英语/
    ├── index.html
    └── ...
```

> 自然科学（科学）目录与数学/语文同构：`科学/index.html` 为学科索引页，课件文件按「学科-年级-单元-ID-知识点.html」扁平存放于 `科学/` 目录下（与数学当前实现一致）。

要点：
- **导航主页** = 根 `index.html`；**学科索引页** = 每学科目录内 `index.html`（每个目录一个 index，双击直达；解决原"index.html 既当导航又当索引"的概念混用）
- **单一数据源** = `assets/knowledge-base.js`；回顾系统逻辑 = `assets/record.js`

### 5.3 文件命名规范

课件文件名格式：`学科-年级-单元-ID-知识点.html`

示例：`数学-三上-第4单元-math-3-012-同分母分数加减法.html`

- 文件名**内嵌知识点 ID**，与 knowledge-base.js 的 `file` 字段一一对应，便于脚本反向校验"文件 ↔ 索引"是否脱节
- 中文可读性保留（搜索、人工管理友好）
- 同目录下不重名；ID 唯一性天然保证不重名
- 共享资源放在 `assets/`，不重复复制

### 5.4 单一数据源：knowledge-base.js 数据结构

#### 5.4.1 为什么用 .js 而不是 .json

课件需在 **`file://` 协议下双击直开**。`fetch('xxx.json')` 在 file 协议下会被浏览器 CORS 拦截（Chrome 直接失败）；而 `<script src="knowledge-base.js">` 无此限制。因此数据源用 **JS 文件定义全局对象**，不用 JSON。

#### 5.4.2 文件放置与加载

- 路径：`assets/knowledge-base.js`
- 加载：所有页面在 `<head>` 末尾 `<script src="../assets/knowledge-base.js"></script>`（相对路径按目录层级调整）
- 作用域：定义全局只读对象 `window.KNOWLEDGE_BASE`，IIFE 包裹，不污染全局

#### 5.4.3 顶层结构

```js
window.KNOWLEDGE_BASE = {
  version: "0.1.0",          // 数据格式版本，改结构时递增
  updatedAt: "2026-08-30",   // 数据最后更新日期
  subjects: [                // 学科字典（供导航主页渲染四科入口）
    { id: "chinese", name: "语文", order: 1 },
    { id: "math",    name: "数学", order: 2 },
    { id: "english", name: "英语", order: 3 }
  ],
  modules: [                 // 知识模块字典（对应 §3.1 四科模块）
    { id: "pinyin",  subject: "chinese", name: "拼音与识字", order: 1 },
    { id: "number",  subject: "math",    name: "数与运算",   order: 1 }
    // …按需补全
  ],
  knowledgePoints: [         // ★ 知识点主表（全部知识点，扁平数组，见 5.4.4）
    // …
  ]
};
```

#### 5.4.4 知识点对象字段字典

对应 §3.3 元数据，并补充索引/搜索/回顾所需字段。**每个知识点一个对象**，压平为一维数组，不按学科/年级嵌套，便于搜索与回顾系统查询。

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| `id` | string | 是 | 知识点 ID，唯一。规则见 5.4.5 | `"math-3-012"` |
| `name` | string | 是 | 知识点名称 | `"同分母分数加减法"` |
| `subject` | string | 是 | 学科 id：`chinese` / `math` / `english` | `"math"` |
| `grade` | number | 是 | 年级 1–6 | `3` |
| `term` | string | 是 | 学期：`"上册"` / `"下册"` | `"上册"` |
| `unit` | string | 是 | 单元（教材口径） | `"第4单元"` |
| `module` | string | 是 | 知识模块 id（对应 modules 字典） | `"number"` |
| `difficulty` | string | 是 | `"基础"` / `"提高"` / `"挑战"` | `"基础"` |
| `source` | string | 是 | 来源：`"syllabus"`（教材全量）/ `"custom"`（按需补充） | `"syllabus"` |
| `prerequisiteIds` | array | 否 | 前置知识点 ID 数组，知识图谱用 | `["math-3-005"]` |
| `status` | string | 是 | `"待制作"` / `"已完成"` / `"待修订"` | `"已完成"` |
| `file` | string | 完成后必填 | 课件相对**课件库根目录**的路径；未完成留 `""` | `"数学/三年级/上册/第4单元/数学-三上-第4单元-math-3-012-同分母分数加减法.html"` |
| `summary` | string | 是 | 一句话简介，**≤30 字**，索引页副标题 | `"同分母分数直接相加，分母不变"` |
| `keywords` | array | 否 | 搜索关键词（近义词、缩写等） | `["分数","加减","分母"]` |
| `createdAt` / `updatedAt` | string | 是 | 创建/更新时间 `YYYY-MM-DD` | `"2026-08-30"` |

#### 5.4.5 ID 规则

- 格式：`{subject}-{grade}-{seq}`
- `subject`：`chinese` / `math` / `english`
- `grade`：1–6
- `seq`：**该学科该年级内的序号**，3 位补零，从 001 递增
- 校验：全局唯一；新增知识点时由维护者保证（可跑校验脚本，见 5.7）

#### 5.4.6 约束与约定

- **展示层不冗余**：索引页的标签（年级/单元/难度）、状态、薄弱点标识均由字段 + record.js 结果即时渲染，不在数据里另存展示字段。
- **派生字段不落库**：如"知识点所属模块名""年级名称（三年级/三上）"由 id/grade/term 推导，避免多份数据不一致。
- **新增/修订流程**：改数据 → 更新 `updatedAt` → 相关页面自动生效，无需改索引页 HTML。
- **来源标记**：教材体系知识点 `source:"syllabus"`（`unit` 对应教材真实单元）；按需补充的 `source:"custom"`（`unit` 可填"专项"或关联的教材单元，`prerequisiteIds` 尽量挂到相关教材知识点，便于纳入知识图谱）。索引页可按来源筛选（教材 / 我的补充）。

#### 5.4.7 示例数据（3 条示意）

```js
knowledgePoints: [
  {
    id: "math-3-012",
    name: "同分母分数加减法",
    subject: "math",
    grade: 3, term: "上册", unit: "第4单元",
    module: "number",
    difficulty: "基础",
    source: "syllabus",
    prerequisiteIds: ["math-3-005"],
    status: "已完成",
    file: "数学/三年级/上册/第4单元/数学-三上-第4单元-math-3-012-同分母分数加减法.html",
    summary: "同分母分数直接相加减，分母不变",
    keywords: ["分数", "加减", "分母"],
    createdAt: "2026-08-20", updatedAt: "2026-08-28"
  },
  {
    id: "chinese-1-001",
    name: "声母 b p m f",
    subject: "chinese",
    grade: 1, term: "上册", unit: "第2单元",
    module: "pinyin",
    difficulty: "基础",
    source: "syllabus",
    prerequisiteIds: [],
    status: "待制作",
    file: "",
    summary: "认识声母 b p m f 的发音与书写",
    keywords: ["声母", "拼音", "b", "p", "m", "f"],
    createdAt: "2026-08-30", updatedAt: "2026-08-30"
  },
  {
    id: "english-3-007",
    name: "be 动词 am is are",
    subject: "english",
    grade: 3, term: "下册", unit: "第3单元",
    module: "grammar",
    difficulty: "提高",
    source: "syllabus",
    prerequisiteIds: ["english-3-002"],
    status: "待制作",
    file: "",
    summary: "掌握 am/is/are 与人称的搭配",
    keywords: ["be动词", "am", "is", "are", "语法"],
    createdAt: "2026-08-30", updatedAt: "2026-08-30"
  }
]
```

### 5.5 配套查询函数（kb-utils.js）

`assets/kb-utils.js` 提供纯查询函数，供索引页/主页/回顾系统复用（不依赖 DOM，便于测试）：

```js
window.KB = {
  get(kpId),                 // 按 ID 查知识点对象；不存在返回 null
  bySubject(subjectId),      // 学科下的全部知识点
  byGrade(subjectId, grade), // 学科 + 年级
  byStatus(status),          // 按状态筛选
  search(text),              // 匹配 name / summary / keywords（大小写不敏感、模糊）
  prereqs(kpId),             // 前置知识点对象数组（不存在 ID 跳过并 console.warn）
  subjectName(subjectId),    // 学科 id → 中文名
  moduleName(moduleId)       // 模块 id → 中文名
};
```

> 说明：查询函数负责"查数据"，不负责"算学习状态"。薄弱点判定属于回顾系统（record.js），避免职责混杂。

### 5.6 索引系统（R6 核心需求）

每个学科、每个知识点必须有**索引条目**，点击条目跳转到对应课件。

#### 5.6.1 索引条目字段

| 字段 | 展示内容 |
|------|----------|
| 知识点名称 | 主标题，加粗显示 |
| 一句话简介 | 副标题，取 `summary`，不超过 30 字 |
| 标签 | 年级、单元、难度（彩色标签；难度色：基础绿/提高黄/挑战红，低饱和） |
| 状态标识 | 已完成（可点击）/ 待制作（灰色不可点，显示"即将上线"）/ 待修订（黄色提示） |
| 薄弱点标识 | 由 record.js 判定为薄弱点的条目加红色圆点徽标 |
| 跳转链接 | 点击条目在新标签页打开对应课件 HTML |

#### 5.6.2 索引页组织方式

- **按学科分组**：语文 / 数学 / 英语 三个分区，可切换 Tab
- **按年级筛选**：每个学科内按 1–6 年级折叠或筛选
- **搜索框**：支持按知识点名称、关键词搜索
- 页面组成：顶部栏（返回主页链接 · 学科名 · 搜索框）→ 筛选区（年级/难度/状态/来源）→ 主体（条目卡片列表）

#### 5.6.3 渲染逻辑（关键约束）

- **全部数据来自 `KNOWLEDGE_BASE`，不在 HTML 里手写任何知识点条目**
- 页面加载流程：`加载 knowledge-base.js → 读取当前学科 → KB.search/byGrade 过滤 → 循环渲染卡片`
- 搜索框输入即时过滤（name/summary/keywords）；筛选条件变化只重渲染列表，不刷新页面

### 5.7 校验脚本（validate-kb.js，建议实现）

Node 或浏览器控制台运行，检查：ID 唯一、`file` 指向的文件真实存在、`status=已完成` 时 `file` 非空、前置知识点 ID 存在——自动保证"索引 ↔ 文件"不脱节。

### 5.8 导航主页

`index.html` 为系统入口，包含：

- 四科大图标入口（点击进入对应学科索引页）
- 最近学习/继续学习（基于本地记录）
- 薄弱点快速复习入口
- 全局搜索框

---

## 6. 回顾系统

### 6.1 职责划分

- `knowledge-base.js`：静态事实（知识点是什么、在哪、前置依赖）
- `record.js`：动态状态（学没学、得分、错题、薄弱点），读 localStorage，回查 knowledge-base 取名称/路径
- 两者解耦：record.js 只存 `kpId`，展示时再 join knowledge-base

### 6.2 学习记录

- 使用浏览器 `localStorage` 存储，无需联网、无需服务器；**单学习者**，不引入多孩子隔离（若未来需支持第二个孩子，再引入 `aiTeacher:learner` 前缀即可）
- localStorage key：`aiTeacher:records`（以 kpId 为键的对象，每个 kpId 存历史数组）

```js
// localStorage: aiTeacher:records = {
//   "math-3-012": [
//     { date: "2026-08-30", done: true, score: 80, timeUsed: 420, wrongIds: ["q3", "q5"] },
//     { date: "2026-08-28", done: true, score: 55, timeUsed: 510, wrongIds: ["q2", "q3", "q7"] }
//   ]
// }
```

字段：知识点 ID、学习日期、是否完成、练习得分、用时、错题（练习题目 ID）。

### 6.3 错题与薄弱点

- 互动练习中答错的题目自动标记为错题，关联到对应知识点
- 薄弱点判定阈值（常量写在 record.js 顶部，便于调整）：

| 规则 | 建议值 |
|------|--------|
| 单次不达标 | 得分 < 60% |
| 判定为薄弱点 | 连续 2 次不达标，或近 5 次中 ≥3 次不达标 |
| 解除薄弱点 | 最近一次得分 ≥ 80% 且无新错题 |

- 索引页中薄弱知识点显示红色圆点标识

### 6.4 复习路径

- 根据薄弱点自动生成"复习推荐"列表，优先复习易错知识点
- 支持按"最近错题""薄弱知识点""未完成"三种维度筛选
- 形成**学（课件）→ 练（互动练习）→ 评（得分记录）→ 复（错题复习）**的闭环
- 复习路径采用**轻量游戏化叙事**（如"收复城池""打怪升级"），降低薄弱点复习的枯燥感

### 6.5 数据流与接口

```
knowledge-base.js（静态事实）
   ├─→ 导航主页 index.html      四科入口统计 / 最近学习 / 全局搜索
   ├─→ 学科索引页 index.html    条目渲染 / 筛选 / 搜索 / 薄弱点红点
   ├─→ 课件 HTML                通过 URL 参数 ?kp=math-3-012 带 ID，结束时写记录
   └─→ record.js（动态状态）    复习推荐 / 错题列表（join 回查知识点名称与路径）
```

课件写入记录建议：播放器在"完成学习"和"每次练习作答"时调用 `record.js` 的 `record.answer(kpId, qId, correct)` 与 `record.finish(kpId, score, timeUsed)`。

---

## 7. 技术选型

| 技术 | 选型与理由 |
|------|------------|
| 前端技术 | 纯 HTML + CSS + JavaScript，无框架、无构建步骤，双击即开 |
| 动画 | CSS 动画 / SVG / Canvas，优先零依赖方案 |
| 语音合成 | Web Speech API（SpeechSynthesis），浏览器内置，零音频文件 |
| 本地存储 | localStorage，记录学习进度和错题（注意 file:// 协议下各浏览器隔离策略差异，实现时做 storage 可用性探测与降级） |
| 外部依赖 | 原则上零外部依赖；如需图表可引入 ECharts（CDN，带降级） |
| 部署方式 | 纯静态文件，本地文件夹即可运行；后续可一键部署为网页 |

---

## 8. 实施路线

| 阶段 | 目标 | 关键产出 |
|------|------|----------|
| **阶段一·搭骨架** | 建立基础设施 | 目录结构、单一数据源（knowledge-base.js + kb-utils.js）、导航主页、学科索引页、步骤播放器通用组件（player.js）、统一样式、3–5 个示范课件跑通全流程 |
| **阶段二·建内容** | 批量产出课件 | 按"4 年级优先 + 低年级补基础"两条线批量产出四科课件，同步沉淀可复用动画组件库，持续更新索引与数据源 |
| **阶段三·强交互** | 完善学习闭环 | 学习记录、错题标记、复习推荐、知识图谱跳转，形成完整的学—练—评—复闭环 |

---

## 9. 课件制作验收检查清单

> 每份课件制作完成后，必须逐项通过以下检查才能标记为"已完成"并加入索引。

| 序号 | 检查项 | 验收标准 |
|------|--------|----------|
| 1 | 结构完整性 | 包含情境引入、动画讲解、例题演示、互动练习、要点小结五段 |
| 2 | 步骤播放器 | 内容拆为有序步骤，每步有动画画面和讲解文本 |
| 3 | 控制栏完整 | 上一步、下一步、播放/暂停、重置、进度条、语速、语音开关全部可用 |
| 4 | 语音讲解 | 每步切换时自动朗读讲解文本，语速可调，可开关，切换时中断上一条语音 |
| 5 | 自动播放 | 点击播放后依次自动推进，可暂停/跳转/重置，播完自动停止 |
| 6 | 进度条可跳转 | 点击进度条任意位置可跳转到对应步骤 |
| 7 | 动画准确 | 动画服务于概念理解，几何关系准确，操作有即时反馈 |
| 8 | 互动练习 | 至少 2 道练习题（题型偏操作），作答即时反馈，错题记入本地记录 |
| 9 | 难度分层 | 练习或例题包含基础/提高/挑战三档，或明确标注适用难度 |
| 10 | 讲解文本显示 | 语音讲解文本始终可见（字幕），关闭语音后仍可阅读 |
| 11 | 文件命名 | 符合"学科-年级-单元-ID-知识点.html"规范，存放于正确目录 |
| 12 | 索引条目 | 索引页中已添加对应条目，字段完整，点击可跳转，状态为"已完成" |
| 13 | 元数据完整 | knowledge-base.js 中该知识点字段已完整填写，与文件对应 |
| 14 | 零依赖运行 | 双击 HTML 文件即可正常运行，不依赖本地服务器 |
| 15 | 移动端可读 | 窄屏下内容不溢出、控件可点击（≥44px）、文字不小于 14px |
| 16 | 专注友好性 | 每步有任务预告句；界面单一视觉焦点；无空转动画；默认无背景音乐；错误提示温和且给二次机会；反馈庆祝 ≤2s；讲解语音有起伏、带提问感 |

---

## 10. 决策记录

### 10.1 已确认决策

| 决策点 | 结论 | 确认时间 |
|--------|------|----------|
| 系统服务对象 | 仅服务自家一个孩子（刚升四年级），无多孩子隔离 | 2026-08-30 |
| 内容策略 | 教材全量清单（1–6 年级四科）+ 学习过程中按需补充（`source:"custom"`） | 2026-08-30 |
| 制作优先级 | 两条线并进：4 年级当前内容（新版教材）+ 低年级基础补缺 | 2026-08-30 |
| 教材版本 | 宁波：部编语文 + 人教版数学 + 人教 PEP 英语（一年级起点，1 年级起开英语课）+ 教科版科学 | 2026-08-30 |
| 英语清单范围 | 从 1 年级开始建（含 1–2 年级英语） | 2026-08-30 |
| 新增学科·自然科学 | 新增第四科「自然科学（科学）」，教科书版，模块按 2022 课标四大领域；4 年级优先（四上按 2026 秋新版） | 2026-08-30 |
| 自动播放停留 | 默认约 2.5 秒（随语速缩放） | 2026-08-30 |
| 游戏化档位 | **轻量档**（星级 + 连击 + 挑战关 Boss 叙事），先做轻量，孩子接受后再加深 | 2026-08-30 |
| 专注友好性 | 已并入课件制作规范（4.9）与验收清单（第 9 项 16） | 2026-08-30 |
| 动画优先 | **知识原理尽量用动画演示形成过程**，已固化进 4.6 动画呈现规范（核心原则） | 2026-08-30 |
| 数据架构 | 单一数据源 knowledge-base.js + 每学科目录内 index.html + 文件名内嵌 ID | 2026-08-30 |

### 10.2 待确认决策

| 决策点 | 选项 | 默认建议 | 状态 |
|--------|------|----------|------|
| 使用场景 | 孩子自学 / 家长陪学 / 教师课堂 | 家长陪学为主 | 待确认 |
| 语音来源 | 浏览器 TTS / 预录音频 / 混合 | 浏览器 TTS 为主，关键课件可替换预录 | 待确认 |
| 访问方式 | 纯本地文件夹 / 部署为网页 | 先本地，后续可部署 | 待确认 |
| 首批清单范围 | 全量 1–6 年级 / 先 4 年级 + 基础点 | 先建全量清单底座，优先落 4 年级与基础点数据 | 待确认 |
| 练习位置 | 嵌入步骤流 / 末尾统一做 | 讲解中穿插小题 + 末尾综合练习 | 待确认 |
| 每日时长上限 | 家长可设 | 默认 20 分钟 | 待家长端实现时定 |

---

## 11. 落地顺序

1. 确认本文档与教材版本（第 10 章）
2. **全量梳理知识点清单**：按教材把小学 1–6 年级四科知识点全量录入 knowledge-base.js（初始多为"待制作"），形成完整知识地图
3. 建目录骨架 + 生成 `knowledge-base.js` 与 `kb-utils.js`（含全量清单首批数据）
4. 建导航主页与四科索引页（纯渲染逻辑，先跑通"数据 → 条目"）
5. 按 **4 年级优先**制作课件（含 3 下衔接、4 上、4 下），validate-kb.js 每次新增后跑一遍
6. 学习过程中遇到的具体问题 → 以 `source:"custom"` 知识点随时补充并制作课件

---

> **本文档为活文档**，随项目推进持续更新。每次新增课件类型、交互模式或组织方式变更时，应先修订本文档，再执行制作。
